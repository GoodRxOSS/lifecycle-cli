/**
 * Generated by orval v7.4.1 🍺
 * Do not edit manually.
 * Lifecycle API
 * API documentation for lifecycle
 * OpenAPI spec version: 2.0.0
 */
import type { UpdateBuildConfigPatchRequestCommentRuntimeEnv } from './updateBuildConfigPatchRequestCommentRuntimeEnv.js';
import type { UpdateBuildConfigPatchRequestCommentInitEnv } from './updateBuildConfigPatchRequestCommentInitEnv.js';

export type UpdateBuildConfigPatchRequest = (unknown & {
  /**
   * The new UUID to assign to the build. Must use lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.
   * @minLength 3
   * @maxLength 50
   * @pattern ^[a-z0-9-]+$
   */
  uuid?: string;
  /** Whether this build should be treated as a static environment. */
  isStatic?: boolean;
  /** Whether pushes to selected default branches should redeploy this build. */
  trackDefaultBranches?: boolean;
  /** Runtime environment override object to replace on the build when provided. */
  commentRuntimeEnv?: UpdateBuildConfigPatchRequestCommentRuntimeEnv;
  /** Init environment override object to replace on the build when provided. */
  commentInitEnv?: UpdateBuildConfigPatchRequestCommentInitEnv;
}) | (unknown & {
  /**
   * The new UUID to assign to the build. Must use lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.
   * @minLength 3
   * @maxLength 50
   * @pattern ^[a-z0-9-]+$
   */
  uuid?: string;
  /** Whether this build should be treated as a static environment. */
  isStatic?: boolean;
  /** Whether pushes to selected default branches should redeploy this build. */
  trackDefaultBranches?: boolean;
  /** Runtime environment override object to replace on the build when provided. */
  commentRuntimeEnv?: UpdateBuildConfigPatchRequestCommentRuntimeEnv;
  /** Init environment override object to replace on the build when provided. */
  commentInitEnv?: UpdateBuildConfigPatchRequestCommentInitEnv;
}) | (unknown & {
  /**
   * The new UUID to assign to the build. Must use lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.
   * @minLength 3
   * @maxLength 50
   * @pattern ^[a-z0-9-]+$
   */
  uuid?: string;
  /** Whether this build should be treated as a static environment. */
  isStatic?: boolean;
  /** Whether pushes to selected default branches should redeploy this build. */
  trackDefaultBranches?: boolean;
  /** Runtime environment override object to replace on the build when provided. */
  commentRuntimeEnv?: UpdateBuildConfigPatchRequestCommentRuntimeEnv;
  /** Init environment override object to replace on the build when provided. */
  commentInitEnv?: UpdateBuildConfigPatchRequestCommentInitEnv;
}) | (unknown & {
  /**
   * The new UUID to assign to the build. Must use lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.
   * @minLength 3
   * @maxLength 50
   * @pattern ^[a-z0-9-]+$
   */
  uuid?: string;
  /** Whether this build should be treated as a static environment. */
  isStatic?: boolean;
  /** Whether pushes to selected default branches should redeploy this build. */
  trackDefaultBranches?: boolean;
  /** Runtime environment override object to replace on the build when provided. */
  commentRuntimeEnv?: UpdateBuildConfigPatchRequestCommentRuntimeEnv;
  /** Init environment override object to replace on the build when provided. */
  commentInitEnv?: UpdateBuildConfigPatchRequestCommentInitEnv;
}) | (unknown & {
  /**
   * The new UUID to assign to the build. Must use lowercase letters, numbers, and hyphens, and cannot start or end with a hyphen.
   * @minLength 3
   * @maxLength 50
   * @pattern ^[a-z0-9-]+$
   */
  uuid?: string;
  /** Whether this build should be treated as a static environment. */
  isStatic?: boolean;
  /** Whether pushes to selected default branches should redeploy this build. */
  trackDefaultBranches?: boolean;
  /** Runtime environment override object to replace on the build when provided. */
  commentRuntimeEnv?: UpdateBuildConfigPatchRequestCommentRuntimeEnv;
  /** Init environment override object to replace on the build when provided. */
  commentInitEnv?: UpdateBuildConfigPatchRequestCommentInitEnv;
});
